# Spike — Mopro FFI build chain (Phase 0)

CEO directive: validate that the ZK build chain works on `stealf-app`
before committing to slices that depend on it (Slice 5 — Stealth/Umbra).

## Background

`front-stealf` ships `modules/mopro-ffi/` (~82 MB, prebuilt
`MoproFfiFramework.xcframework`). That folder is the **build source**
for the npm package `@umbra-privacy/rn-zk-prover` — it is *not* what
runs in production. Production resolves the pod via:

```
# front-stealf/ios/Podfile.lock
MoproFfi:
  :path: "../node_modules/@umbra-privacy/rn-zk-prover"
```

So replicating the runtime setup on `stealf-app` only requires the
public npm package — the 82 MB module folder stays in `front-stealf`.

## What was validated (autonomous, in this session)

| Step | Status | Evidence |
|------|--------|----------|
| Public npm visibility of `@umbra-privacy/rn-zk-prover@3.0.1` | OK | `npm view @umbra-privacy/rn-zk-prover version` → `3.0.1` |
| Public npm visibility of `@umbra-privacy/sdk@4.0.0` | OK | `npm view @umbra-privacy/sdk version` → `4.0.0` (Slice 5) |
| `npm install @umbra-privacy/rn-zk-prover` | OK | Resolved in 6 s, no peer dep failures |
| Metro bundle still builds with the package added | OK | `expo export --platform ios --dev` → 23.5 MB bundle, no resolver errors |
| Podspec ships with the npm package | OK | `node_modules/@umbra-privacy/rn-zk-prover/MoproFfi.podspec` exists |
| `expo prebuild --platform ios --no-install --clean` | OK | `ios/` generated, Podfile uses `use_native_modules!` autolinking |
| `newArchEnabled` (Turbo Module prerequisite) | OK | `app.json` already has `"newArchEnabled": true` |
| `.zkey` registered as Metro asset extension | OK | `metro.config.js` patched (Slice 5 prereq) |

## What needs Thomas's machine to validate

The native build chain (Xcode + CocoaPods + Hermes JSI) requires a
physical macOS toolchain. Three steps are left for the demo:

1. **Pod install**
   ```bash
   cd ios && pod install
   # expect MoproFfi (3.0.1) in Podfile.lock, sourced from
   # ../node_modules/@umbra-privacy/rn-zk-prover
   ```

2. **iOS build**
   ```bash
   cd .. && npx expo run:ios
   # or `xcodebuild -workspace ios/testclaudedesign.xcworkspace ...`
   ```

3. **Run a minimal proof to validate the JSI bridge**
   The `@umbra-privacy/rn-zk-prover` API surface lets us call a low-cost
   prover. We hold off on writing the JS test until Slice 5 since
   adding it would require pulling Umbra circuit `.zkey` assets into
   the repo for a one-shot test — wasted effort.

   The pragmatic check during the demo: if `expo run:ios` boots the
   app on the simulator without a Mopro-related Hermes / linker error,
   the Turbo Module is wired correctly. A no-op `installRustCrate()`
   call from a `__DEV__` block on first render would confirm the
   bridge actively loads (~2 s).

## Verdict format

| Outcome at demo | Verdict |
|-----------------|---------|
| Pod install + xcodebuild + simulator boot all succeed | 🟢 ship Phase 0 |
| Pod install OK but xcodebuild fails on Mopro symbols | 🟡 dig in (probably codegen/Folly mismatch — known issue space) |
| Pod install fails (autolink misses `MoproFfi`) | 🔴 escalate — repo state diverges from front-stealf in a way we did not catch |

## Where things live now

- `package.json` — `"@umbra-privacy/rn-zk-prover": "^3.0.1"` (Phase 0)
- `metro.config.js` — `.zkey` in `assetExts`
- `app.json` — `newArchEnabled: true` (already set when Thomas scaffolded the project)
- `ios/` — gitignored, regenerated by `expo prebuild`
