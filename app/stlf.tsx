import { StlfProductScreen } from '@/src/features/reflect/screens/StlfProductScreen';

// STLF (Reflect) product detail — pushed from the Earn screen's "Available products".
export default function StlfProductRoute() {
  return <StlfProductScreen />;
}
